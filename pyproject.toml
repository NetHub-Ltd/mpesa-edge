[project]
name = "mpesa-edge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "workers-py>=1.9",
]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
